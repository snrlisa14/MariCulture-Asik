<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Penyakit</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="css/style.css" />
    <style>
      .symptom-checkbox {
        margin-bottom: 10px;
      }

      .symptom-row {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
      }

      .symptom-column {
        flex: 1 1 calc(25% - 15px);
        box-sizing: border-box;
      }

      .btn-submit {
        width: 100%;
        padding: 10px;
        font-size: 16px;
      }

      /* Responsive Design */
      @media (max-width: 768px) {
        .symptom-column {
          flex: 1 1 calc(50% - 15px);
        }
      }

      @media (max-width: 576px) {
        .symptom-column {
          flex: 1 1 100%;
        }
      }
    </style>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="wrap-none p-4">
      <div class="container-fluid">
        <h1
          class="text-center text-white"
          style="font-weight: 700; margin-bottom: 30px"
        >
          Identifikasi Penyakit dan Masalah Kesehatan Spesies
        </h1>

        <div class="m-auto" style="width: 100%">
          <div class="about-container mt-3 mb-5">
            <form action="/identifikasi" method="POST">
              <h4>Pilih Gejala yang Dirasakan:</h4>
              <br>
              <br>
              <div class="form-group">
                <div class="symptom-row">
                  <% symptoms.forEach(function(symptom) { %>
                  <div class="symptom-column">
                    <div class="form-check symptom-checkbox">
                      <input
                        class="form-check-input"
                        type="checkbox"
                        name="symptoms"
                        value="<%= symptom.id %>"
                        id="symptom<%= symptom.id %>"
                      />
                      <label
                        class="form-check-label"
                        for="symptom<%= symptom.id %>"
                      >
                        <%= symptom.symptom %>
                      </label>
                    </div>
                  </div>
                  <% }); %>
                </div>
              </div>
              <br>
              <br>
              <div class="d-flex justify-content-center">
                <button
                  type="submit"
                  class="btn btn-primary button-blue btn-submit"
                >
                  Identifikasi
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
